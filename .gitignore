.DS_Store
Thumbs.db

# Python
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# Cross-repo planning docs live outside this repo's tree, so they can't normally
# end up here — this is a defensive backstop in case one is ever copied in by
# mistake. Never commit these.
4-repo-migration-PLAN.md
4-repo-migration-PROMPT.md

# Standing AI-agent instructions - kept local-only by design, never pushed.
# Claude Code reads this from the filesystem regardless of git tracking, so
# it still auto-loads every session without needing to be in the repo.
CLAUDE.md
